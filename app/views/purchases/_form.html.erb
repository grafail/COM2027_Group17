<%= form_with(model: purchase, local: true) do |form| %>
  <% if purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
      <% purchase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :TicketID %>
    <%= form.number_field :TicketID %>
  </div>

  <div class="field">
    <%= form.label :Comments %>
    <%= form.text_field :Comments %>
  </div>

  <div class="field">
    <%= form.label :PriceTotal %>
    <%= form.text_field :PriceTotal %>
  </div>

  <div class="field">
    <%= form.label :PurchaserID %>
    <%= form.number_field :PurchaserID %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
